# 基于粗粒度意图辅助与长短序列一致性学习的早期骨架动作识别研究计划

## 1. 研究题目
基于粗粒度意图辅助与长短序列一致性学习的早期骨架动作识别研究

## 2. 当前定位
这是一条独立的 early skeleton 子课题，目标不是继续做关节点剪枝与部署优化，而是围绕“动作尚未完成时能否尽早识别”建立一套完整研究线。

当前判断如下：

1. 早期动作识别并不是空白问题，但在骨架场景下仍然没有完全固定的主流方案。
2. 已有工作大多集中在多观察比例统一建模、未来信息预测、或完整序列教师指导部分序列学习。
3. 对骨架数据而言，如果方法依赖逐帧阶段标签、子动作切分或复杂流程，迁移成本较高。
4. 因此，本课题的切入点应明确收敛为：`coarse intent auxiliary + full-prefix consistency + no frame-level annotation`。

## 3. 研究目标
本研究拟实现以下四个目标：

1. 构建标准化的早期骨架动作识别设定，使模型只能看到动作前缀而非完整序列。
2. 在动作分类之外引入粗粒度意图辅助任务，帮助模型在低观察比例下更早锁定行为方向。
3. 利用完整动作分支作为训练期稳定参考，通过长短序列一致性约束提升前缀预测稳定性。
4. 在 `NTU60` 与扩展阶段的 `NTU120` 上验证方法的有效性，并明确收益来源。

## 4. 核心研究问题与假设

### 4.1 核心研究问题
1. 在仅观察动作前 `10%` 到 `50%` 的条件下，骨架模型的主要误判来源是什么？
2. 相比具体动作标签，粗粒度意图是否在更早阶段更容易判别？
3. 完整动作分支能否作为训练期教师，帮助前缀分支学到更稳定的判别边界？
4. 在不增加逐帧标注成本的前提下，粗粒度意图辅助是否能够稳定提升低观察比例性能？

### 4.2 工作假设
1. `H1`：粗粒度意图在早期阶段比具体动作类别更早显现，因此可作为有效辅助监督。
2. `H2`：完整序列与前缀序列之间的一致性学习能够降低早期预测的过早偏置。
3. `H3`：`intent` 与 `consistency` 的联合收益将主要体现在低观察比例，尤其是 `0.1` 和 `0.3`。
4. `H4`：若不同意图映射方案都能带来稳定收益，则说明结论不依赖单一主观分组。

## 5. 研究范围与协议冻结

### 5.1 数据集范围
当前阶段先锁定如下范围：

- 主数据集：`NTU60`
- 首轮评测协议：`XSub`、`XView`
- 扩展验证：`NTU120`
- 扩展协议：`XSub`、`XSet`

### 5.2 早期识别设定
每个样本只保留动作前缀作为输入，并插值到统一长度。首轮固定以下观察比例：

- `0.1`
- `0.3`
- `0.5`
- `0.7`
- `0.9`
- `1.0`

说明：

1. `1.0` 保留为完整动作上界与训练参考，不作为 early 方法的核心卖点。
2. 主结果重点报告 `0.1/0.3/0.5`，因为这些比例最能体现早期识别难度。
3. 若训练成本过高，首轮开发可先用 `0.3/0.5/0.7/1.0` 建立稳定基线，再补 `0.1/0.9`。

### 5.3 当前阶段结论
当前阶段不是大规模跑实验，而是先完成协议冻结，避免后续出现下列漂移：

- 观察比例定义不一致
- 前缀裁剪与随机裁剪混用
- `NTU60` 与 `NTU120` 混合叙述
- `full accuracy` 与 `early accuracy` 混合比较

## 6. 方法设计

### 6.1 整体思路
以 `SkateFormer` 为主干，围绕同一样本构造两路输入：

- 前缀序列分支
- 完整序列分支

前缀分支负责 early action prediction；完整分支只在训练期提供稳定参考，不增加测试期推理负担。

### 6.2 粗粒度意图辅助
在动作标签之外引入一层 coarse intent 标签。该标签不追求高度细化，而强调：

- 稳定
- 可解释
- 易于人工整理
- 可迁移到 `NTU60/NTU120`

首轮计划构造 `8` 到 `12` 类意图标签，并准备至少 `2` 套分组方案做稳健性验证。

候选意图大类包括：

- 自我行为
- 姿态与移动
- 手势表达
- 读写与交流
- 吃喝与生活行为
- 拿取与放置物体
- 开关与操作物体
- 穿戴与整理
- 人际互动
- 激烈互动

### 6.3 长短序列一致性学习
对同一样本的前缀分支和完整分支施加一致性约束，使前缀输出向完整输出靠拢。

一致性约束的候选实现包括：

1. logits 一致性
2. soft target / KL 一致性
3. 中间特征一致性

首轮实现应优先从最简单的 `logits` 或 `KL` 一致性开始，避免方法过重。

### 6.4 模型输出
共享主干后设置两个预测头：

- 动作分类头
- 粗粒度意图分类头

完整分支可与前缀分支共享头部结构，但训练职责不同：

- 前缀分支：动作分类 + 意图分类 + 一致性约束
- 完整分支：动作分类监督 + 教师参考

## 7. 损失函数与训练策略

### 7.1 损失函数
总损失定义为：

`L = L_action + λ1 * L_intent + λ2 * L_consistency`

其中：

- `L_action`：动作分类损失
- `L_intent`：粗粒度意图分类损失
- `L_consistency`：前缀分支与完整分支之间的一致性损失

首轮超参数从以下设置开始：

- `λ1 = 0.3`
- `λ2 = 0.5`

### 7.2 训练流程
训练分为三个阶段：

1. 完整动作预训练  
   先用 `1.0` 完整序列训练稳定主干，获得 full 分支参考模型。

2. 前缀基线训练  
   仅使用前缀序列，建立 `SkateFormer-prefix` 与 `multi-ratio` 基线。

3. 联合训练  
   加入 `intent` 与 `consistency`，形成完整方法。

### 7.3 课程式训练
为避免超低观察比例在初期拉低训练稳定性，采用逐步加难策略：

1. 先用 `0.5/0.7/0.9/1.0`
2. 再加入 `0.3`
3. 最后加入 `0.1`

如果训练资源有限，课程式训练优先级高于一次性混合全部观察比例。

## 8. 基线、对比与消融

### 8.1 基线组
首轮至少需要以下基线：

- `SkateFormer-full`
- `SkateFormer-prefix`
- `SkateFormer-prefix + multi-ratio`

### 8.2 方法对比组
为了明确收益来源，核心对比组固定为：

- `SkateFormer-prefix + intent`
- `SkateFormer-prefix + consistency`
- `SkateFormer-prefix + KD-only`
- `Full model`

说明：

1. `KD-only` 用于对照“是否只要完整序列教师就足够”。
2. 如果 `consistency` 已经等价于简单 `KD`，则在论文中要明确术语，不要重复命名。
3. `Full model` 的收益必须在上述单模块对比组上成立，方法才有说服力。

### 8.3 消融重点
重点分析以下因素：

- 是否加入粗粒度意图辅助
- 是否加入长短序列一致性
- 不同一致性形式的影响
- 不同意图映射方案的影响
- 不同损失权重的影响
- 不同观察比例训练策略的影响

## 9. 评价指标与分析维度

### 9.1 主要指标
- 不同观察比例下的 `Top-1 Accuracy`
- 多观察比例平均准确率
- 最早预测正确的观察比例
- 粗粒度意图分类准确率

### 9.2 辅助分析
- 低观察比例与高观察比例的收益差异
- 不同动作大类上的提升差异
- 前缀分支与完整分支预测分布差异
- 易混淆类别对的错误迁移情况

### 9.3 重点观察对象
预计最值得分析的类别包括：

- 人际互动类
- 物体操作类
- 起始阶段动作模式相似、后期才分化的类别

## 10. 当前阶段的可执行任务

### 10.1 第一优先级
1. 锁定 early setting 的数据处理协议  
   明确有效帧统计、前缀裁剪、插值方式、统一长度和观察比例脚本。

2. 整理 `NTU60` 的 coarse intent 映射表  
   至少产出 `2` 套方案，例如 `8` 类版本和 `10` 类版本。

3. 跑通 `SkateFormer-prefix` 基线  
   先在 `XSub` 上建立首轮 early baseline，再扩到 `XView`。

### 10.2 第二优先级
1. 加入双头结构与 `intent-only` 实验
2. 加入 `consistency-only` / `KD-only` 实验
3. 确认低观察比例是否存在稳定收益

### 10.3 第三优先级
1. 扩展到 `NTU120`
2. 做跨协议验证
3. 补充可视化、错误案例和论文图表

## 11. 预期创新点的收敛表述
当前版本不再把创新点写得过满，而收敛为以下三点：

1. 面向早期骨架动作识别，将 coarse intent auxiliary 与 full-prefix consistency 进行联合建模。
2. 在不依赖逐帧阶段标注、子动作切分和复杂流程的前提下，构建轻量 early skeleton 识别方案。
3. 在 `NTU` 系列数据上系统分析不同观察比例下的收益来源，特别是低观察比例场景。

注意：

- 不宜宣称“首次将层级语义引入骨架识别”，因为层级语义与 coarse-to-fine 思路在相关领域已有先例。
- 更稳妥的表述应强调“用于 early skeleton recognition 的组合方式与轻量设定”。

## 12. 可能风险与应对

### 风险 1：粗粒度意图标签存在主观性
应对方式：设计两到三套映射方案，并报告方案间稳定性。

### 风险 2：收益只集中在个别观察比例
应对方式：同时报告各观察比例结果、平均结果和最早预测正确指标，避免单点结论。

### 风险 3：收益主要来自教师指导而非意图辅助
应对方式：设置 `KD-only`、`intent-only`、`consistency-only`、`multi-ratio-only` 等对照组。

### 风险 4：方法在 `NTU60` 有效，但扩展到 `NTU120` 不稳定
应对方式：先把 `NTU60` 结论做扎实，再将 `NTU120` 作为外部验证，而不是一开始双线并跑。

### 风险 5：方法复杂度增加但测试阶段收益有限
应对方式：严格保证完整分支只参与训练，测试阶段只保留前缀分支，避免额外推理负担。

## 13. 当前版本的阶段安排

### 阶段 P0：协议冻结
- 锁定 `NTU60 XSub/XView`
- 锁定观察比例与前缀裁剪逻辑
- 锁定 early baseline 训练脚本

### 阶段 P1：基线建立
- 训练 `SkateFormer-prefix`
- 跑通 `multi-ratio` 基线
- 得到首轮各观察比例结果

### 阶段 P2：单模块验证
- 加入 `intent-only`
- 加入 `consistency-only`
- 加入 `KD-only`

### 阶段 P3：完整模型
- 联合训练 `intent + consistency`
- 完成主结果表与消融表
- 完成不同动作大类的错误分析

### 阶段 P4：扩展与写作
- 扩展到 `NTU120`
- 固化最终图表
- 撰写论文摘要、引言、方法与实验

## 14. 一句话总结当前计划
当前这条研究线已经从“泛泛的想法”收敛为一个明确的 early skeleton 方案：先锁定 `NTU60` early protocol，再建立 `SkateFormer-prefix` 基线，然后用 `coarse intent auxiliary` 和 `full-prefix consistency` 分别做单模块验证，最后再做联合模型与扩展实验。
