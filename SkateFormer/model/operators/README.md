# Operators

This directory stores operator-level changes for the acceleration branch.

Current intended use:

- `ACmix`-inspired shared-projection operators
- lightweight aggregation operators for `SkateFormer`
- small reusable blocks extracted from `SkateFormer.py`

Keep experimental operator code here instead of expanding the main model file further.
