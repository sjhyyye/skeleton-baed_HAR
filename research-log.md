# Research Log

Chronological record of research decisions and actions. Append-only.

| # | Date | Type | Summary |
|---|------|------|---------|
| 1 | 2026-05-06 | bootstrap | Initialized the workspace around the earlier rehabilitation and deployment-oriented skeleton action-recognition direction. |
| 2 | 2026-05-06 | inner-loop | Recorded prior pruning evidence from the older acceleration line, including 25-point and 19-point operating results. |
| 3 | 2026-05-06 | outer-loop | The earlier main plan focused on pruning, compression, and Jetson deployment as the dominant next steps. |
| 4 | 2026-05-07 | pivot | Deprecated the acceleration-and-deployment plan as the central research line. The project was re-scoped to early skeleton action recognition with coarse intent supervision and long-short sequence consistency. |
| 5 | 2026-05-07 | planning | Rewrote the central `research-plan.md` around the early-recognition problem, locking the new primary benchmark to NTU60 early recognition rather than deployment latency. |
| 6 | 2026-05-07 | planning | Updated the central state tracking so that protocol freeze, prefix baseline construction, and single-module ablations are now the immediate next actions. |
| 7 | 2026-05-07 | planning | Rewrote `early_skeleton_research_plan.md` into an execution-oriented plan covering protocol freeze, baseline setup, intent mapping, consistency variants, and phased evaluation. |
| 8 | 2026-05-07 | environment | Recorded that all `SkateFormer` training and evaluation commands should run inside the `conda` environment `skateformer`. |
| 9 | 2026-05-12 | repository-hygiene | Unified repository-facing experiment docs around the early-recognition main line and demoted the older pruning/compression/deployment thread to a short historical note. |
| 10 | 2026-06-23 | baseline-sync | Synchronized the central project state after completing the `r=0.1` single-ratio rerun. `H1` is now treated as substantially complete on `NTU60 XSub`: `prefix_multi` beats matched single-ratio baselines at both `0.1` (`31.88%` vs `27.66%`) and `0.3` (`68.92%` vs `67.04%`), so the next priority shifts to `KD-only` and `consistency-only` rather than further baseline work. |
| 11 | 2026-07-05 | intent-ablation | Completed the first `intent-improved` check on top of the stronger trajectory mapping using ratio-adaptive intent weights and intent-to-action bias/gating. The new row reaches `31.30% / 68.48% / 85.53%` at `0.1 / 0.3 / 0.5`, slightly improving the fixed trajectory row at `0.3 / 0.5` but still failing to beat `prefix_multi` on the decisive low-ratio comparisons. |
| 12 | 2026-07-13 | intent-ablation | Completed the full six-ratio evaluation of `early_observable_v2` intent-only. It reaches `31.53% / 68.33% / 85.46% / 90.27% / 91.39% / 91.55%` at `0.1 / 0.3 / 0.5 / 0.7 / 0.9 / 1.0`. This is a small improvement over the fixed trajectory mapping on average, but it still does not beat `prefix_multi` on the decisive low-ratio comparisons (`31.53 < 31.88` at `0.1`, `68.33 < 68.92` at `0.3`). The conclusion is that early-observable grouping is directionally better than earlier intent taxonomies, but intent grouping refinement alone is still insufficient; the next priority remains `KD-only` and `consistency-only`. |
| 13 | 2026-07-27 | branch-pivot | On the new branch, deprecated early skeleton action recognition as the central line of work and restored compute acceleration as the main objective. The new reference paper is `On the Integration of Self-Attention and Convolution (arXiv:2111.14556)`. |
| 14 | 2026-07-27 | planning | Rewrote the top-level repository narrative so that `README.md`, `research-plan.md`, `research-state.yaml`, `findings.md`, and `literature/survey.md` now define the branch in terms of acceleration, profiling, and Pareto validation rather than partial-prefix recognition. |
| 15 | 2026-07-27 | planning | Replaced the Chinese early-recognition execution plan with `acceleration_research_plan.md` and reclassified the older early-recognition experiment folders as archived material for this branch. |
| 16 | 2026-07-27 | planning | Narrowed the paper direction further: `ACmix` remains design inspiration, but the main proposed method is now framed as a `partition-free / branch-collapsed / cat-proj-free` `SkateFormer` block redesign for pruned low-`V` inference, rather than a generic shared-projection transplant. |
